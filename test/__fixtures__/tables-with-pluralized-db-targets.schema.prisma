datasource db {
  provider = "sqlite"
  url      = "file:database.db"
}

// generator
generator client {
  provider = "prisma-client-js"
}

model User {
  id Int @id
  name String
}

model Group {
  id Int @id
  name String
}
